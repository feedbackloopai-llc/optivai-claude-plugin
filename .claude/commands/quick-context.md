**Purpose**: Fast context loading for experienced agents
**Usage**: `/quick-context`

```
Prompt Template: "Quick FeedbackLoopAI context: Internal productivity platform. PostgreSQL (Neon) with pgvector for embeddings and persistent memory. Python for data pipelines and Claude Code hooks. Activity logging via Claude Code hooks -> PostgreSQL. Core rules: SQL-first, log to DataFixLog not markdown. Key files: README.md, CLAUDE.md. Architecture: modular data pipelines, Open Brain memory system, Beads task tracking. Follow security patterns and data integrity standards. I have persistent memory (Neon PostgreSQL + pgvector) and task tracking (Beads). Run /brain-context to recall recent decisions, people, and patterns before starting work. Ready for tasks."
```
