# Core dependencies for OptivAI Claude PostgreSQL Plugin

# PostgreSQL connector
psycopg2-binary>=2.9.0

# pgvector support for PostgreSQL
pgvector>=0.2.0

# Local embeddings (all-mpnet-base-v2, 768-dim)
sentence-transformers>=2.2.0

# Claude API for metadata extraction
anthropic>=0.30.0

# YAML support for Memory System (optional but recommended)
PyYAML>=6.0

# RFC 6902 JSON Patch for RB primitive (brain-W1-S5: snapshot diffs, rollback)
jsonpatch>=1.32

# Beads CLI dependencies (scripts/beads/cli.py, storage.py)
# These are declared in setup.py but must also appear here so that
# `pip install -r requirements.txt` (the Windows and standalone path) covers them.
click>=8.0
filelock>=3.0
