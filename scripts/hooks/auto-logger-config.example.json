{
  "enabled": true,
  "log_tool_operations": true,
  "log_user_prompts": true,
  "max_prompt_length": 500,
  "session_tracking": true,
  "log_level": "info",
  "log_directory_mode": "global",
  "global_log_path": "~/.claude/logs",
  "project_log_subdir": ".claude/logs",
  "destinations": {
    "local": {
      "enabled": true
    },
    "postgresql": {
      "enabled": true,
      "connection_url_env": "DATABASE_URL",
      "target_table": "landing.raw_events",
      "tenant_id": "CLAUDE_CODE",
      "source_system": "CLAUDE_CODE",
      "auth": {
        "type": "connection_string",
        "_comment": "Uses DATABASE_URL env var (Neon PostgreSQL)"
      },
      "sync": {
        "batch_size": 100,
        "flush_interval_seconds": 60,
        "retry_attempts": 3,
        "retry_delay_seconds": 5
      }
    }
  }
}
