**Purpose**: Fast context loading for experienced agents
**Usage**: `/quick-context`

```
Prompt Template: "Quick OptivAI context: Enterprise Business SaaS platform. FastAPI backend (port 8000) + React frontend (port 80). AWS Cognito auth + JWT. File organization: docs/ reports/ artifacts/ tools/ temp/ - NEVER use root. Key files: README.md, API_MAPPING_DOCUMENT.md, FILE_ORGANIZATION_RULES.md (root). Architecture: hybrid dev (AWS auth + local services), full AWS prod. Core feature: LLM-driven business artifact generation. Follow security patterns and organized file structure. Ready for tasks."
```